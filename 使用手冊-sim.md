# 使用手冊-sim — Solow 比較靜態

由 teach-sim 生成於 2026-09-21；AI 協助生成，完成教材待授課教師審核。

## 老師端

### 重生成與修改

觸發句：`用「teach-sim」這個 skill，我想做一個 Solow 比較靜態的互動教材給大學部總體班。`

修改前先更新 `sdd-archive/2026-09-21-solow-comparative-statics/` 的規格，再要求依新規格調整教材並複驗。`index.html` 包含全部樣式、圖形及程式；不需安裝套件即可使用。

### 模型與課程校準

連續時間、封閉經濟、無政府、無技術進步，A = 1、α = 1/3。k、y、c 為每位勞工的資本、產出與消費；n 是勞動人口成長率，時間單位為年。

- y = A k^α
- dk/dt = sAk^α − (n+δ)k
- 正穩態 k* = [sA/(n+δ)]^(1/(1−α))
- y* = A(k*)^α；c* = (1−s)y*
- 黃金律 s = α，只最大化穩態消費，未考慮過渡成本與時間偏好。

預設 s = 20%、n = 1%、δ = 5%，正穩態 k* = 6.0858、y* = 1.8257、c* = 1.4606。參數是教學示範，不是台灣估計值。儲蓄率可調 5%–80%、人口成長率 0%–5%、折舊率 2%–10%；步距 0.1 個百分點。「定位黃金律」使用精確 1/3，顯示約 33.33%。

圖上的交點縱座標是投資 sy*，不是產出 y*；原點是另一個邊界穩態。比較圖自動縮放，應以同一畫面的基準與目前線條比較。資本不會因拉桿立即跳到新穩態，這裡顯示的是長期結果。無技術進步時，正穩態每人產出成長率為零，總產出成長率為 n。

### 課堂流程（15–20 分鐘）

1. 先問「儲蓄越多，長期消費一定越多嗎？」（2 分鐘）
2. 任務一：預測三項結果，再把 s 從 20% 調至 30%；看投資線上移。（4 分鐘）
3. 任務二：分別提高 n 至 3% 或 δ 至 8%；看維持資本線變陡。（4 分鐘）
4. 任務三：比較 s = 20%、1/3、60%；c* 為 1.4606、1.5713、1.2649，打破「儲蓄越多消費越多」的直覺。（5 分鐘）
5. 學生記錄本次比較，匯出 CSV，口頭說明水準與成長率的差別。（2 分鐘）

任務先提交預測才解鎖控制項，提交時會重設參數與基準。看解釋按鈕給指定題目的答案，不自動評分任意多參數實驗。教師可用自由探索直接示範，或設目前值為新的比較基準。重設不刪除紀錄與代號。

### 預覽、Git 收尾與發布

直接打開 `index.html`；也可在本資料夾執行 `python3 -m http.server 8000`，以瀏覽器開啟 http://localhost:8000 。整份資料夾可壓縮交付。

Git 與 Pages 操作詳見 [README](README.md)。本次已初始化 main 分支；因系統缺少 Git 姓名與 email，初始 commit 未完成，請依 README 設定自己的身分後提交。推送自己建立的 GitHub repo 後，在 Settings → Pages 選 main 分支的根目錄。此版本沒有班級共享模式，不需要 token；要查看全班結果，請由學生透過既有課程平台繳交各自 CSV，教師再於試算表彙整。本機紀錄不會自動同步到 GitHub。

公開前教師應完成下方審核，再將頁尾「待授課教師審核」改為實際審核狀態；不要只因提案或設計通過，就宣稱成品已經審核。

## 學生端

### 取得與操作

開啟教師提供的網頁連結或 index.html。選任務 → 完成三項方向預測 → 開始操作 → 拉動參數 → 看解釋。若想改預測，按「重啟任務」。選自由探索則可直接操作。滑桿可用鍵盤方向鍵，按鈕與選單可用 Tab 到達。手機圖表區可左右滑動，結果也列於數值表格。

### 保存與匯出

代號選填，限 20 字，不填姓名或學號。只有按「記錄本次比較」才新增一筆；調拉桿不自動新增。匯出 CSV 後依教師指示繳交。資料僅留在同一瀏覽器與來源，不會跨裝置同步；清除瀏覽器資料、使用無痕模式或更換檔案位置，可能使紀錄無法保留。

若看到「僅本次開啟保留」，請在關閉頁面前匯出。損壞的舊紀錄不會被自動覆寫；可先匯出本次資料，再清除本教材紀錄重新開始。清除需確認，不影響其他網站資料；若刪除失敗，介面會提醒舊資料可能仍在。

CSV 共 16 欄：

| 欄位 | 意義 |
|---|---|
| time | ISO 時間（UTC） |
| nickname | 淨化後的選填代號 |
| task | 任務名稱／自由探索 |
| prediction | 提交時的三項預測／自由探索 |
| baseline_s, baseline_n, baseline_delta | 基準參數，以小數表示，例如 0.2 = 20% |
| baseline_k, baseline_y, baseline_c | 基準正穩態 |
| s, n, delta | 目前參數，以小數表示 |
| k, y, c | 目前正穩態 |

CSV 為含 BOM 的 UTF-8，繁中可供試算表讀取。可能被試算表解讀成公式的文字加單引號前綴；這是資料保護，不是代號輸入錯誤。

## 審核紀錄

- 提案：2026-09-21，使用者回覆「繼續」。
- 設計與任務：2026-09-21，使用者回覆「go」。
- 軟體驗證：詳見歸檔 tasks.md 的實測紀錄；軟體測試不代替教師學科與課堂適切性審核。
- [ ] 老師已審核學科正確性（日期：＿＿＿＿　審核者：＿＿＿＿）
- [ ] 需修正處：＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿
- [ ] 修正後已重跑／複核，確認可用（日期：＿＿＿＿）
